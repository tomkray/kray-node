# THE LIGHT DOOR

**Baptize: `lightdoor`**
**Action:** Keep fruitless criminal harm out of the atlas — before the
book, before the disk — so the library stays light. Not a ministry of
speech.

## The Formula

> Given the Foundation (each person's energy is their own; no one exploits anyone),
> A5 (a written star cannot be unread), and the Creator's doctrine (this network
> exists to propagate genuine prosperity — not to host fruitless criminal harm);
> and given that we are **against censorship** (no taste committee, no admin un-write,
> no ban of art, politics, or ugly speech);
> operate a vision at the **unique writer only**, reading **this relic**;
> the model returns exactly `ALLOW` or `REFUSE`;
> `REFUSE` happens **before** `node.submit` and **before** any byte under `content/`;
> so the book never holds the hash and the atlas never holds the body —
> and a stranger never mistakes this door for a ministry of speech.

The destination is inside the frequency: **light, not silence.**

## One small rule, iterated

```
z ← the body at the door
c ← this relic (the doctrine on stone)
if the body is fruitless criminal harm → REFUSE (drop; never journal; never store)
else → ALLOW (the book and the atlas may receive it)
```

Every zoom uses the same `c`. A new prompt that bans taste has changed `c` —
that is a different network. Changing `c` means a new sealed star, not a
quieter edit.

## Why this shape, not another

- Does energy reach its owner, or is it extracted? Hosting
  exploitation is extraction. The door keeps the current clean.
- The LLM is a third party **at the door only**. The reducer never
  asks it. The book never contains its verdict. The third party
  cannot write a leaf.
- A hash once submitted is a leaf. Refuse before the root would
  have to carry it.
- The model is not a perfect witness. Unsure on the closed set →
  `REFUSE`. We do not claim the network is pure. We try, with a
  tool, so the library stays light.
- The same closed set for every address.
- Honesty is not the attacker's equilibrium — so the door is the wall.
- HALT: `ALLOW` | `REFUSE`. No essay. No “maybe” on disk.
- The refuse set is short. Every added category is a new `c`.
- Sealed so it stands when we are gone.
- One movement: doctrine → rule → halt.

## This is not censorship

KRAY.NETWORK has no ministry of speech. A sealed star stays. Politics,
adult art, satire, dissent, ugly words — the door does not touch them.

The closed edge is criminality that adds nothing and harms people:
sexual exploitation of children (anyone 17 or under — photoreal,
illustration, generated); non-consensual sexual violence depicted as
real abuse; content whose sole purpose is to traffic, extort, or
sexually exploit a person. When unsure whether a person is a child →
`REFUSE`.

If a future mouth uses this relic to silence a voice it dislikes, that
mouth has left the Formula.

## The verdict

```
ALLOW
REFUSE <one-line reason, no description of what was seen>
```

On `REFUSE`: do not `node.submit`; do not write `content/`; drop the
buffer. Vision down or timeout on the closed set: `REFUSE`. A name-only
or law-only act (no body) does not enter vision.

## Stone

Inscribe this file. Baptize `lightdoor`. After the seal, the running
vision prefers these bytes. The copy under `atlas-guard/` is the same
doctrine on disk until then.
