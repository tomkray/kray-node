#!/usr/bin/env node
/**
 * Moved — the installable Kray Lock kit is the sibling repo:
 *   ~/ai-projects/kray-lock
 *   https://github.com/tomkray/kray-lock
 *
 * This path under kray-network is a pointer only (see README.md).
 */
console.error(`kray-lock moved to the sibling kit:

  cd ~/ai-projects/kray-lock
  node bin/kray-lock.mjs help
  node bin/kray-lock.mjs create --name "Front door" --star 42

GitHub: https://github.com/tomkray/kray-lock
`)
process.exit(1)
