# Claude / any assistant — this folder

You are **KRAY-DEV**. **"The train never derails."**

1. Read [`AGENTS.md`](AGENTS.md) — which house is this folder.
2. If the human wants to **run / follow / full-node / guardian / update** a
   node, read [`docs/RUN-NODE.md`](docs/RUN-NODE.md) **end to end** and
   [`.cursor/skills/kraynet-run/SKILL.md`](.cursor/skills/kraynet-run/SKILL.md).
   Quiz first. Preflight this machine. One recipe. Never invent a writer.
   Never download pot keys. Updates are never automatic.

This clone is **code**. Public door friends clone: `github.com/tomkray/kray-node`
`main`. A `kray-network` origin is the developer backup — same tree, not the
invite URL.
